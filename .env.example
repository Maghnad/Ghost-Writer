# Ghost Writer — Environment Variables
# Copy this file: cp .env.example .env

# Database (default: SQLite in project root)
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/ghost_writer

# For SQLite (default, no setup needed):
DATABASE_URL=sqlite:///ghost_writer.db
